{
  "mcpServers": {
    "tutor-tools": {
      "command": "/bin/bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/run_server.sh"],
      "env": {
        "TUTOR_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
